@model IEnumerable<AdvocaciaPPFinal.Models.Advogado>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Advogados</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<div class="row">
    <div class="col-md-6">
        <h3>
            Advogados Cadastrados
        </h3>
    </div>
    <div class="col-md-6">
        <form asp-controller="Advogados" asp-action="Index">
            <p>
                <select method="get" name="SearchOption">
                    <option value="Nome">Nome</option>
                    <option value="CPF">CPF</option>
                    <option value="Inscricao">Inscrição</option>
                </select>
                <b>Title: </b> <input type="text" name="SearchString" method="get">
                <input class="btn btn-dark" type="submit" value="Filter" />
            </p>
        </form>
    </div>
</div>

<br />
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome_Advogado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CPF_Advogado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Inscricao_Advogado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Especializacao_Advogado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Numero_casos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome_Advogado)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CPF_Advogado)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Inscricao_Advogado)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Especializacao_Advogado)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Numero_casos)
                </td>
                <td>
                    <a class="btn btn-success" asp-action="Edit" asp-route-id="@item.Id_Advogado">Edit</a> |
                    <a class="btn btn-warning" asp-action="Details" asp-route-id="@item.Id_Advogado">Details</a> |
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id_Advogado">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
